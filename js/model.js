'use strict';












  /*let last_left = pointer.offsetLeft;

  if (last_left < cur_el.offsetLeft) {
    pointer.style.right = spy_menu.offsetWidth - cur_el.offsetLeft - cur_el.offsetWidth + "px";

    setTimeout(function () {
      pointer.style.left = cur_el.offsetLeft + "px";
    }, 100);
  } else if (last_left > cur_el.offsetLeft) {
    pointer.style.left = cur_el.offsetLeft + "px";

    setTimeout(function () {
      pointer.style.right = spy_menu.offsetWidth - cur_el.offsetLeft - cur_el.offsetWidth + "px";
    }, 100);
  }*/

